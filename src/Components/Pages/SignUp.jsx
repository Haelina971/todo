import IDForm from "../Forms/IDForm";

const SignUp = () =>  {
    //Look how to make a form reusable just different labels
    return (
        <IDForm action="Sign Up"/>
    );
}

export default SignUp;